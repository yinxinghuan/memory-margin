import {createHash,randomUUID} from 'node:crypto'
import {mkdir,readFile,writeFile} from 'node:fs/promises'
import {dirname} from 'node:path'
import {generateImageMedia,MediaServiceError} from './media-service'

const sessionId='b198e25d-5781-48c3-930a-143ed23a92b4'
const first=JSON.parse(await readFile('assets/benchmark/generated-room/generation-report.json','utf8')) as {results:Array<{id:string;url?:string}>}
const scannerUrl=first.results.find(result=>result.id==='audit-resonance-scanner')?.url
if(!scannerUrl)throw new Error('MISSING_SCANNER_REFERENCE')
const jobs=[
 {id:'audit-comparison-console-v2',output:'assets/benchmark/generated-room/audit-comparison-console-v2.png',prompt:'NEW COMPLETE OBJECT using this accepted low circular scanner only as the exact steep top-down B-view camera, short-body proportion, pixel density, graphite/brass/teal material and light reference. Create ONE low DUAL MEMORY COMPARATOR, a wide rounded-rectangle floor device with a broad top occupying at least 88 percent of its visible height, no legs and only a thin dark lower rim. On the top are two circular recessed wafer wells, two short abstract cyan waveform strips, and one small central amber physical toggle. A standing adult is about 105 source pixels tall; footprint about 145 by 85 pixels, visible body height no more than 24 pixels. All straight edges parallel, zero convergence or near-side enlargement. Center one complete new device on uniform pure RGB #FF00FF. No reuse of scanner rings, no table, legs, upright screen, text, glyphs, person, floor, wall, hologram, cast shadow, product photo or perspective.'},
 {id:'audit-sealed-buffer-v2',output:'assets/benchmark/generated-room/audit-sealed-buffer-v2.png',prompt:'NEW COMPLETE OBJECT using this accepted low circular scanner only as the exact steep top-down B-view camera, low-body proportion, pixel density, graphite/brass/teal material and light reference. Create ONE low SEALED MEMORY EVIDENCE BUFFER, a compact rounded-square floor capsule with a broad top occupying at least 88 percent of visible height, no legs and only a thin lower rim. The top contains two inset translucent wafer cassettes beneath a dark protective glass strip and one tiny amber tamper seal with no mark. A standing adult is about 105 source pixels tall; footprint about 100 by 82 pixels, body height no more than 22 pixels. Parallel edges, zero convergence, no near-side enlargement. Center one complete new device on uniform pure RGB #FF00FF. No scanner rings, upright cabinet, drawers, legs, text, glyphs, person, floor, wall, hologram, cast shadow, product-photo camera or perspective.'},
] as const
const startedAt=new Date().toISOString(),batchStart=performance.now()
const results=await Promise.all(jobs.map(async job=>{const requestId=randomUUID(),start=performance.now();try{const task=await generateImageMedia({sessionId,requestId,mode:'edit',prompt:job.prompt,referenceUrls:[scannerUrl],size:{width:512,height:512}});const generatedAt=performance.now(),response=await fetch(task.media.url);if(!response.ok)throw new Error(`DOWNLOAD_HTTP_${response.status}`);const bytes=Buffer.from(await response.arrayBuffer()),downloadedAt=performance.now();await mkdir(dirname(job.output),{recursive:true});await writeFile(job.output,bytes);return {id:job.id,status:'succeeded' as const,requestId,taskId:task.task_id,url:task.media.url,output:job.output,size:[task.media.width,task.media.height],serviceTimingMs:task.timing_ms??null,generationMs:Math.round(generatedAt-start),downloadMs:Math.round(downloadedAt-generatedAt),totalMs:Math.round(downloadedAt-start),sha256:createHash('sha256').update(bytes).digest('hex'),prompt:job.prompt}}catch(error){const e=error as Error;return {id:job.id,status:'failed' as const,requestId,totalMs:Math.round(performance.now()-start),error:e instanceof MediaServiceError?{code:e.code,message:e.message,retryable:e.retryable,retryAfterSeconds:e.retryAfterSeconds}:{code:'LOCAL_ERROR',message:e.message,retryable:false},prompt:job.prompt}}}))
const report={version:1,room:'memory-authenticity-audit',mode:'parallel-low-device-correction',sessionId,startedAt,endedAt:new Date().toISOString(),wallClockMs:Math.round(performance.now()-batchStart),concurrency:jobs.length,results}
await writeFile('assets/benchmark/generated-room/correction-report.json',JSON.stringify(report,null,2)+'\n');console.log(JSON.stringify(report));if(results.some(result=>result.status==='failed'))process.exitCode=1
